from common import toolbox as tb
from common import folders

settings = 'common/settings.json'
# Providers JSON
providers = '{}/providers.json'.format(folders.metadata)
# Predictions JSON
predictions = '{}/predictions.json'.format(folders.metadata)
# Stations JSON
stations = '{}/stations.json'.format(folders.metadata)
# Centers JSON
centers = '{}/centers.json'.format(folders.metadata)
# Cities JSON
cities = '{}/cities.json'.format(folders.metadata)
# Names JSON
names = '{}/names.json'.format(folders.metadata)
# Updates JSON
updates = '{}/updates.json'.format(folders.metadata)
